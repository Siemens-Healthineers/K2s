# SPDX-FileCopyrightText: © 2023 Siemens Healthcare GmbH
# SPDX-License-Identifier: MIT

# CI Pipeline for K2s Unit Tests
name: CI - Unit Tests

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths-ignore:
      - 'LICENSES/**'
      - 'bin/**'
      - 'doc/**'
      - 'k2s.exe'
      - '*.md'
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - 'LICENSES/**'
      - 'bin/**'
      - 'k2s.exe'

env:
  GOVERSION: "1.21.5"

jobs:
  run-unit-tests:
    name: Run Unit Tests
    timeout-minutes: 30
    continue-on-error: false
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{env.GOVERSION}}
        cache-dependency-path: "**/*.sum"

    - name: Show all diff
      run: |
        git diff --exit-code -- .

    - name: Show Source Files
      shell: pwsh
      run: ls -r

    - name: Run Unit Tests
      shell: powershell
      run: |
        $ErrorActionPreference = "Continue"
        .\k2s\test\execute_all_tests.ps1 -Tags unit -V -ThrowOnFailure
