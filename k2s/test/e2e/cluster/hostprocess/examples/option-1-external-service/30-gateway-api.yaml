# SPDX-FileCopyrightText: © 2026 Siemens Healthineers AG
#
# SPDX-License-Identifier: MIT

# External routing for Option 1 with the ingress traefik addon (Gateway API).
# Enable it first:  k2s addons enable ingress traefik
#
# End-to-end flow:
#   curl client -> traefik ingress (172.19.1.100) -> albums-external Service
#                -> native albumswin.exe on 172.19.1.1:8080
#
# The traefik addon runs the Gateway API provider (gatewayClassName: traefik) and
# exposes its 'web' entrypoint (container port 8080) as :80 on 172.19.1.100.
# The listener binds the shared K2s hostname k2s.cluster.local, so callers must use
# that Host header (it resolves to 172.19.1.100 on the K2s host).
#
# Call it:  curl.exe -v http://k2s.cluster.local/albums-win
---
# GatewayClass for Traefik. The traefik addon does NOT ship one, and Traefik does
# not auto-create it, so the Gateway below would never be Accepted without this.
# Cluster-scoped + idempotent: harmless if it already exists.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: traefik
spec:
  controllerName: traefik.io/gateway-controller
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: albums-gateway
  namespace: hostprocess-examples
spec:
  gatewayClassName: traefik
  listeners:
    - name: web
      protocol: HTTP
      hostname: k2s.cluster.local # shared K2s hostname (-> 172.19.1.100)
      port: 8080 # traefik 'web' entrypoint (published as :80 on 172.19.1.100)
      allowedRoutes:
        namespaces:
          from: All
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: albums-external
  namespace: hostprocess-examples
spec:
  parentRefs:
    - name: albums-gateway
      namespace: hostprocess-examples
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /albums-win # forwarded unchanged to albumswin.exe
      backendRefs:
        - name: albums-external # selectorless Service in front of 172.19.1.1
          namespace: hostprocess-examples
          port: 80
